# OS / editor
.DS_Store
Thumbs.db
desktop.ini
*~
.vscode/
.idea/

# Dependencies / build (none yet — static site)
node_modules/
dist/
build/

# Local env / secrets — never commit these
.env
.env.*
*.key
*.pem
secrets/
.enclave-live/
poc/enforcement-seam/audit-ledger.jsonl

# desktop build
desktop/node_modules/
desktop/dist/
desktop/.stage/
varvel/.tmp/

# spark-code (python agent) — source only, no venv/build/local state
spark-code/.build-venv/
spark-code/__pycache__/
spark-code/**/__pycache__/
spark-code/*.exe
spark-code/sessions/
spark-code/_testout.txt
spark-code/f.txt
spark-code/._*
.spark-code/
