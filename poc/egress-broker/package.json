{
  "name": "enclave-egress-broker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Deny-all egress broker that injects the broker-held API key outside the sandbox (BYO-key without the key ever entering the sandbox).",
  "scripts": {
    "demo": "node demo.mjs"
  }
}
