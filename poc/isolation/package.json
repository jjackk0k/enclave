{
  "name": "enclave-isolation",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Provider-abstracted sandbox manager: provision -> PEP-gated exec -> isolated run -> teardown. Process/Docker/Firecracker tiers.",
  "scripts": {
    "demo": "node demo.mjs"
  }
}
