# CORS reflects arbitrary Origin WITH credentials on Google-IAP-fronted admin host (admin.semrush.net)

> Submission readiness: **READY** — validator-gate validated and fresh.

**Severity:** medium  
**VARVEL severity:** med · **confidence:** CONFIRMED  
**Reported to:** (generic program)

## Summary

CORS reflects arbitrary Origin WITH credentials on Google-IAP-fronted admin host (admin.semrush.net). Affected asset: `admin.semrush.net`. Severity medium, confidence CONFIRMED, validation state validated.

## Steps to Reproduce

1. Reach `admin.semrush.net` (the finding's host in the campaign surface).
2. TODO(validate) — exact endpoint/parameter (the finding carries no ref; write the precise request from your validated session)
3. Reproduce the proof exactly as the validator gate did: oracle unnamed.
4. TODO(validate) — full impact demonstration (state-changing proof is operator sign-off territory; attach only the verified exchange)

## Evidence

stored evidence:

```
PROBE 1 (baseline, no Origin): GET https://admin.semrush.net/ -> HTTP/1.1 302 Found; Location: https://accounts.google.com/o/oauth2/v2/auth?client_id=555419171242-llages1idsfuuhqqtqd65gh0nqb6fd1o.apps.googleusercontent.com&...&redirect_uri=https://iap.googleapis.com/v1/oauth/clientIds/555419171242-llages1idsfuuhqqtqd65gh0nqb6fd1o.apps.googleusercontent.com:handleRedirect&code_challenge_method=S256... (host is fronted by Google Identity-Aware Proxy; pre-auth redirect to Google OAuth with PKCE; Set-Cookie: [REDACTED]
```

_1 secret-bearing value(s) withheld as [REDACTED] per the OPSEC doctrine — the platform records THAT withholding happened, never the value._

## Impact

TODO(validate) — concrete impact statement (derive from the VERIFIED repro at CONFIRMED confidence: what does a successful exploit READ, CHANGE, or REACH? The static record does not state impact, so this report does not either)

## Remediation

- Enforce server-side authorization on every sensitive route; deny by default.
- Maps to: A01:2021 Broken Access Control · MITRE TA0001 Initial Access

## Scope Attestation

TODO(validate) — scope attestation (the input carried no signed scope; attach the program scope + authorization before submission — a report without an attestation reads as unscoped testing)

## Honesty — unverified gaps

- confidence is 'CONFIRMED' — the platform did not confirm this finding end-to-end
- 1 secret-bearing value(s) REDACTED from the evidence per the OPSEC doctrine (secrets/cookies/tokens never reach a report) — kinds: cookie-header
- no signed scope in the input — the attestation line could not be rendered from evidence
- no researcher handle supplied (--researcher) — the platform credit line is a TODO, never a fabricated name

---
_Generated by VARVEL bountyreport. Evidence is verbatim-minus-redactions; gaps are listed, never hidden; readiness is computed from the validator gate, never asserted._