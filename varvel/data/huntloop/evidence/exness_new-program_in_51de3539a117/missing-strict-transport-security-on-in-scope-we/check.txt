const ev = globalThis.EVIDENCE || {}; const targets = ['exness.com', 'exnessaffiliates.com', 'pay.ibex.exchange']; const missing = (ev.assets || []).filter(function(a) { const hasHsts = a.headers && Object.keys(a.headers).some(function(k) { return k.toLowerCase() === 'strict-transport-security'; }); return targets.indexOf(a.host) !== -1 && !hasHsts; }).map(function(a) { return a.host; }).sort(); if (missing.length === 3 && missing[0] === 'exness.com' && missing[1] === 'exnessaffiliates.com' && missing[2] === 'pay.ibex.exchange') { console.log('MISSING_HSTS:exness.com,exnessaffiliates.com,pay.ibex.exchange'); } else { console.log('MISSING_HSTS:' + missing.join(',')); } process.exit(0);