# VARVEL Handoff — 2026-08-30 (for the next agent)

You are continuing work on VARVEL, Jack's authorized red-team / bug-bounty platform.
Jack = the operator. Always HITL for submissions (he clicks Submit), gates are
auto-approved via the gate-keeper under his standing authorization (see below).

## Workspace & environment

- Workspace root: `C:\Users\Jack\Downloads\enclave`
- VARVEL: `C:\Users\Jack\Downloads\enclave\varvel`
- Memory log: `C:\Users\Jack\Downloads\varvel-kimi\KIMI-NOTES.md` — append a dated
  entry after EVERY milestone. Read the tail of it first; it's the project history.
- node is NOT on Git Bash PATH: `export PATH="/c/Program Files/nodejs:$PATH"` in every
  Bash call that needs node.
- Hidden server launches: `wscript C:\Users\Jack\Downloads\enclave\run-hidden.vbs
  C:\Users\Jack\Downloads\enclave\varvel\.tmp\<bat>` — ABSOLUTE paths only (relative
  silently no-ops). taskkill via Git Bash: `taskkill //PID <pid> //F` (in python
  subprocess list-form use `/PID`, not `//PID`).
- NEVER run full `npm test` (pre-existing env failure in test/inlineexec.test.mjs).
  Run individual test files. Never claim unmeasured results.
- Never touch `varvel/.data/` or `varvel/data/` live files except the documented
  precedent edits (settings.json ghost blocks, bountyline/intel.jsonl appends).

## Live instances (loopback consoles)

- :8971 — tripcom locale follow-on RUNNING (37 locale hosts, carryForward from the
  main campaign's 22 hygiene findings; engagement "tripcom", sess-1004, 52 CIDRs).
- :8975 — blendlabs DONE (6 findings, 0 validated, cookie-flag class = excluded by
  Blend policy; nothing filable). Instance session: sess-1006.
- :8976 — agoda DONE (2 findings, 0 validated, TLS class; nothing filable).
  Instance session: sess-1007. Path-scope guard live: pathPrefixes ["/book/"].
- :8977 — Enclave control API (instance 1 lifecycle: POST /api/varvel/stop,
  /api/varvel/open {persona:"red",model:"k3",effort":"high"}).
- Ghost chain (both/all instances): socks5://10.64.0.1:1080, pinned exit
  135.136.21.33. ALWAYS verify POST /api/ghost {"action":"check"} returns
  verified.ok && exitIp match BEFORE launching any campaign — fail-closed.
- If an instance looks quiet >10 min, check /api/state `stall` field FIRST (silent
  freezing is a banned failure mode; stall detector parks visibly as stalled:<phase>).

## Standing authorizations & doctrine (Jack, 2026-08-29/30 — recorded)

- All-phase gate approval on all campaigns: delegated to the GATE-KEEPER automation
  `automation_9fe9f18a-364d-4755-abdb-3b0290f0ad3c` (condition-polls :8971/:8975/:8976
  every 5m; approves parked gates via POST /api/approve {phase}; logs to
  varvel/data/bountyline/gatekeeper-log.jsonl; pings Jack). When adding a 4th instance,
  extend INSTANCES in its two asset scripts and update the automation description.
- Automation-vs-AI doctrine: VARVEL is an AI working at human cadence, gated + scoped —
  NOT automation. Programs silent on automation → human-cadence OK. Explicit
  "no automation" text → engine requires operator ack (Jack's blanket ack stands).
- PROFIT DOCTRINE (bountyline intel.jsonl): fast-lane single/narrow-host programs
  (Semrush shape, <1h) take priority; estate wildcard sweeps only when fast lane empty.
- SUBMISSIONS: only validator/oracle-PROVEN findings. Never file hygiene noise
  (TLS/cookie/header findings are excluded by virtually all program policies).
  Submission path: `varvel/tools/submit-drive.mjs` (no browser extension needed),
  via the HAI route (hackerone.com/hai/report_assistant/...) so H1's AI reviews the
  draft first; submit only after HAI clears it. NEVER mention "varvel" or internal
  tooling in reports. All target requests carry header `X-Hackerone: varvel`.
- Severity honesty: score attack surface + victim pool, not demo depth (Semrush
  lesson — filed Medium, HAI said High 8.6, corrected).

## Completed hunts (all recorded via tools/cli.mjs bountyline run)

- semrush: Medium→High 8.6 submitted H1 #290218 (awaiting triage) — our only filing.
- bykea: 101 findings, 0 validated — nothing filable. State exported:
  varvel/data/exports/bykea-final-state-2026-08-30.json
- blendlabs: 6 findings, 0 validated — nothing filable (cookie flags excluded).
  Report: varvel/data/exports/blendlabs-report-2026-08-30.md
- agoda: 2 findings, 0 validated — nothing filable.
  Report: varvel/data/exports/agoda-report-2026-08-30.md
- tripcom main: 22 findings, 0 validated. Locale follow-on running now.

## CRITICAL KNOWN GAP — fix before/with next campaigns

Both new-campaign agents were CRIPPLED: dozens of `enclave-denied (blendlabs/agoda)`
holds — the campaign AI tried to author scratch tools (its brief tells it to) but the
new workspaces aren't provisioned in the Enclave, so local tools (Bash/Read/Write/Grep)
were refused. FIX: provision enclave workspaces for new engagements (see
enclave/server.mjs ensureWorkspace + PERSONA config; marcus persona ws mapping) so
campaign agents keep full tool-authoring. Without this, agents hunt one-handed.

## Campaign launch recipe (per new program)

1. Pick from `varvel/data/h1watch/state.json` (592 programs, scope arrays included).
   Fast-lane filter: offersBounties && scope.in.length<=4 && not yet hunted.
2. Read rules: `varvel/data/h1watch/outbox/<handle>-new-program-*.json` has the FULL
   policy text + structured_scopes. Confirm what's excluded; note bounty table.
3. Resolve target A records; sign seam session: copy `varvel/.tmp/issue-blendlabs.mjs`
   (imports signSession from poc/enforcement-seam/util.mjs), new sess-id, workspace
   name, engagementScope=/32 list → poc/enforcement-seam/session/marcus-<ws>.json.
4. Add "<ws>" ghost block to varvel/data/settings.json (mirror "blendlabs").
5. New instance: copy varvel/.tmp/serve-blendlabs.mjs + start-blendlabs.bat with a new
   VARVEL_PORT (8978+ free) and ENCLAVE_SESSION. Launch hidden (absolute paths!).
6. Ghost check fail-closed → POST /api/campaign (copy body shape from
   varvel/.tmp/restart-both-fixed.py; single-host budget maxNoise ~250;
   approveTimeoutMs:0; tooledRecon:true; carryForward:false; extraHeaders
   {"X-Hackerone":"varvel"}; if scope is a PATH not a host, use scope.pathPrefixes —
   guard shipped 2026-08-30, engine/scopepath.mjs).
7. Register: node tools/cli.mjs program import <outbox json> --platform hackerone
   --out data/bountyline/<h>-program.json && bountyline add <file>.
8. When done: export /api/state + /api/report to data/exports/, then
   node tools/cli.mjs bountyline run <id> --scope <signed scope copy> --campaign
   <surface.json> [--report <report.md>].

## NEXT BUILDS (in priority order — start a sub-agent to build them)

Full analysis: varvel/docs/research/crit-class-analysis-2026-08-30.md (cited).
1. **BUILD 1 (go first): productize the two-account authorization oracle** —
   IDOR/BOLA is the #1 critical-paying class ($5K–$25K documented). We have the
   differential oracle (tools/idorprobe.mjs) + IDOR primitive family; the build is
   two-session plumbing: harvest IDs from two owned accounts, cross-replay,
   escalate read→write→cross-tenant→mass-assignment, wire into validate/exploit
   phases. Lab-proven standard: pinned tests + campaign-path proof + honest gaps.
2. Build 2: OOB listener + SSRF sink hunter (blind SSRF is currently invisible to us).
3. Build 3: HTTP/2 single-packet race upgrade for tools/race.mjs (H1.1-only now).
4. Also open: hyp-010 (cross-target generalization of logicdiscover), payload-phrasing
   sweep (hyp-004), H2/TLS staging for race, primitive coverage for
   CORS/self-XSS/open-redirect/JWT verticals.

## Sub-agent usage

Jack authorizes the Agent tool. For engine builds, resume coder agent `agent-2`
(it holds chainforge/logiclab/race/stall-fix/scopepath context). Test discipline:
failing-then-passing pins, individual test files, zero new deps, lab-only loopback.

## Watch items

- Semrush H1 #290218 awaiting triage.
- Madara verdict watch: Varvel@proton.me, ticket f26140c9.
- H1 API token rotation is Jack's manual task.
- Restore poc/enforcement-seam/session/marcus.json.bak-20260829 when tripcom ends.
